apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-history
  name: mongo-history
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
